{
  "name": "cognitive-groceries",
  "version": "1.0.0",
  "description": "Groceries scanner",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cloudant": "*",
    "express": "*",
    "request": "*"
  }
}
